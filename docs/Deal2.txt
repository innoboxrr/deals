// Entidad principal

    Deal
        - name
        - description
        - payload (json) (nullable)
        - deal_product_id
        - workspace_id

    DealProduct
        - name
        - description
        - price
        - deal_id

    DealAgreement
        - name
        - description
        - payout (json) (nullable)
        - deal_id
        - deal_customer_id

    DealCustomer
        - name
        - status

// Adminsitración de anuncios

    DealAdCampaing
        - name
        - platform
        - payload

    DealAdGroup
        - name
        - payload
        - deal_ad_campaing_id

    DealAd
        - name
        - payload
        - deal_ad_group_id

    DealAdAudit
        - payload
        - deal_ad_id

// Adqusición

    DealGateway
        - payload (json) (nullable)
            - fixed_cost (para affiliados)
        - gateway_type
        - gateway_id
        - deal_id